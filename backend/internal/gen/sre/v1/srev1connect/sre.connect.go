// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: sre/v1/sre.proto

package srev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/yourname/ai-sre/backend/internal/gen/sre/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SREServiceName is the fully-qualified name of the SREService service.
	SREServiceName = "sre.v1.SREService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SREServiceChatProcedure is the fully-qualified name of the SREService's Chat RPC.
	SREServiceChatProcedure = "/sre.v1.SREService/Chat"
)

// SREServiceClient is a client for the sre.v1.SREService service.
type SREServiceClient interface {
	// Chat streams back explanation, the resolved command, live kubectl output,
	// and a terminal Done chunk — in that order.
	Chat(context.Context, *connect.Request[v1.ChatRequest]) (*connect.ServerStreamForClient[v1.ChatResponse], error)
}

// NewSREServiceClient constructs a client for the sre.v1.SREService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSREServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SREServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	sREServiceMethods := v1.File_sre_v1_sre_proto.Services().ByName("SREService").Methods()
	return &sREServiceClient{
		chat: connect.NewClient[v1.ChatRequest, v1.ChatResponse](
			httpClient,
			baseURL+SREServiceChatProcedure,
			connect.WithSchema(sREServiceMethods.ByName("Chat")),
			connect.WithClientOptions(opts...),
		),
	}
}

// sREServiceClient implements SREServiceClient.
type sREServiceClient struct {
	chat *connect.Client[v1.ChatRequest, v1.ChatResponse]
}

// Chat calls sre.v1.SREService.Chat.
func (c *sREServiceClient) Chat(ctx context.Context, req *connect.Request[v1.ChatRequest]) (*connect.ServerStreamForClient[v1.ChatResponse], error) {
	return c.chat.CallServerStream(ctx, req)
}

// SREServiceHandler is an implementation of the sre.v1.SREService service.
type SREServiceHandler interface {
	// Chat streams back explanation, the resolved command, live kubectl output,
	// and a terminal Done chunk — in that order.
	Chat(context.Context, *connect.Request[v1.ChatRequest], *connect.ServerStream[v1.ChatResponse]) error
}

// NewSREServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSREServiceHandler(svc SREServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	sREServiceMethods := v1.File_sre_v1_sre_proto.Services().ByName("SREService").Methods()
	sREServiceChatHandler := connect.NewServerStreamHandler(
		SREServiceChatProcedure,
		svc.Chat,
		connect.WithSchema(sREServiceMethods.ByName("Chat")),
		connect.WithHandlerOptions(opts...),
	)
	return "/sre.v1.SREService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SREServiceChatProcedure:
			sREServiceChatHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSREServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSREServiceHandler struct{}

func (UnimplementedSREServiceHandler) Chat(context.Context, *connect.Request[v1.ChatRequest], *connect.ServerStream[v1.ChatResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("sre.v1.SREService.Chat is not implemented"))
}
